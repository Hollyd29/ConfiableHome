export const url = "https://comfiable-homes.onrender.com/api/v1";

{
  /* <Dropdown
  data={data}
  labelField="label"
  valueField="value"
  onChange={(item: any) => handleChange(item)}
  style={styles.dropdownHolder}
  itemTextStyle={{ fontSize: 14, margin: -10 }}
  containerStyle={{ marginBottom: 16, borderRadius: 8 }}
  placeholder="_Make a selection_"
  placeholderStyle={styles.placeholderStyle}
  maxHeight={200}
  value={dropdownValue}
/>; */
}
